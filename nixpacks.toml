providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["echo 'No build step needed for Express API'"]

[start]
cmd = "cd backend && npm start"
